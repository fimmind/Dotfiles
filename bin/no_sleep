#!/usr/bin/env fish

function turn_off
  xset s off -dpms
  echo Turned DPMS off
end

function turn_on
  xset s on +dpms
  echo Turned DPMS on
end

function int_handler --on-signal INT
  echo; turn_on; exit
end

turn_off
read -l -P 'Print anything to turn it back on...'
turn_on
